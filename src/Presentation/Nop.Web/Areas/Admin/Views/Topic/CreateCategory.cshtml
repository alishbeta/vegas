@model TopicCategoryModel

@{    
    //page title
    ViewBag.Title = T("Admin.ContentManagement.TopicCategories.AddNew").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("Topic categories");
}

<form asp-controller="Topic" asp-action="CreateCategory" method="post">
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Admin.ContentManagement.TopicCategories.AddNew") 
            <small>
                <i class="fa fa-arrow-circle-left"></i>
                <a asp-action="List">@T("Admin.ContentManagement.TopicCategories.BackToList")</a>
            </small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.SaveContinue")
            </button>
            @*@await Component.InvokeAsync("AdminWidget", new { widgetZone = AdminWidgetZones.TopicCategoryDetailsButtons })*@
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdateCategory", Model)
</form>